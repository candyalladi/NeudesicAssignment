<UserControl x:Class="AssignmentApp.Views.AssignmentAppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssignmentApp.Views"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Column="0" Grid.Row="0" Height="25">
            Create New File
        </Button>
        <Button x:Name ="OpenExistingFileButton" Grid.Column="0" Grid.Row="1" Height="25" 
                Click="OpenExistingFileButton_Click">
            Open Existing
        </Button>
        <ListView Grid.Row="2" ItemsSource="{Binding Records}"></ListView>
        <Button Grid.Column="0" Grid.Row="3" Height="25">
            New Record
        </Button>
        <Button Grid.Column="0" Grid.Row="4" Height="25"
                Command="{Binding SaveCommand}">
            Save
        </Button>
        <Label Grid.Column="1" Grid.Row="0">
            Editor - Create a New or Edit an existing Record
        </Label>
        <StackPanel Grid.Column="1" Grid.Row="1" Grid.RowSpan="3">
            <DatePickerTextBox/>            
            <TextBox/>
            <Label>New Features:</Label>
            <TextBlock x:Name="txtEditor"/>
        </StackPanel>        
    </Grid>
</UserControl>
